---
  - name: clock configuration
    hosts: 10.193.195.14
    connection: local
    gather_facts: no

    tasks:

     - name: IOS 
        command:
           - show clock
       register: clock
     - debug: msg="{{ clock.stdout }}"
