---
  - name: clock configuration
    hosts: '5548'
    connection: local
    gather_facts: no

    tasks:
       - name: IOS | Show clock
         ios_command:
           commands:
            - show clock
         register: clock
